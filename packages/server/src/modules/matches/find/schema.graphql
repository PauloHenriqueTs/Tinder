type Matches {
  id: ID!
  name: String!
  pictureUrl: String
  description: String!
  latitude: Float!
  longitude: Float!
  user: User!
  likes: [String!]
  deslikes: [String!]
  itsMatch: [String!]
}

type Query {
  findMatches: [Matches!]!
}
